# UkrainianLetterUsage Diagnostic Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a diagnostic `UkrainianLetterUsage` that flags Ukrainian-specific letters (`і ї є ґ`) in comments and identifiers, leaving Russian and English untouched.

**Architecture:** A token-scanning diagnostic extending `AbstractDiagnostic`. One `check()` pass over `getTokensFromDefaultChannel()` (filtered to `IDENTIFIER` tokens) plus `getComments()`, flagging any token whose text contains a configurable set of letters (default `[іїєґ]`, matched case-insensitively so uppercase `І Ї Є Ґ` are covered). String literals are not scanned.

**Tech Stack:** Java, Spring (annotation-driven bean discovery), ANTLR (`BSLParser` tokens), JUnit 5 + AssertJ, Gradle with the `io.github.1c-syntax.bslls-dev-tools` plugin (scaffolding + doc/schema generation).

---

## Key facts (verified against the codebase)

- The diagnostic is auto-discovered via the `@DiagnosticMetadata` annotation (Spring bean scan in `DiagnosticInfosFactory`). **No manual registry edit is needed.**
- `gradlew newDiagnostic` scaffolds 7 files but: writes the tag in lowercase (`DiagnosticTag.suspicious` — does **not** compile), extends `AbstractVisitorDiagnostic`, omits the license header, and leaves stub bodies. All of these get replaced below. The scaffolder asks for tags on **stdin**, so pipe them in.
- `generateDiagnosticDocs` is wired into the `classes` task and **fails the build if the docs `.md` files are missing**. The docs files must exist before any compile/test. (Task 1 creates minimal ones; Task 6 fills them.)
- `DiagnosticInfosTest` (runs in full `check`) requires: non-empty `diagnosticName` and `diagnosticMessage` (ru + en), a non-empty description resource (sourced from the docs `.md`), 1–3 tags, and a ru + en resource description for **every** `@DiagnosticParameter` (here: `letters`).
- Config-test API (from `BadWordsDiagnosticTest`): `diagnosticInstance.info.getDefaultConfiguration()` returns a mutable map; `.put(...)` then `diagnosticInstance.configure(map)`, then `getDiagnostics()`.
- The diagnostic ranges and counts asserted below were captured from a live test run, not estimated.

## File Structure

- Create: `src/main/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic.java` — the diagnostic.
- Create: `src/main/resources/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic_ru.properties` — Russian name/message/param.
- Create: `src/main/resources/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic_en.properties` — English name/message/param.
- Create: `docs/diagnostics/UkrainianLetterUsage.md` — Russian docs.
- Create: `docs/en/diagnostics/UkrainianLetterUsage.md` — English docs.
- Create: `src/test/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnosticTest.java` — tests.
- Create: `src/test/resources/diagnostics/UkrainianLetterUsageDiagnostic.bsl` — fixture.
- Regenerated by Gradle (do not hand-edit): `docs/diagnostics/index.md`, `docs/en/diagnostics/index.md`, `src/main/resources/com/github/_1c_syntax/bsl/languageserver/configuration/schema.json`, `.../configuration/parameters-schema.json`, and the auto-header block of the two docs `.md` files.

---

### Task 1: Scaffold and reduce to a compiling, zero-diagnostic skeleton

**Files:**
- Create (via scaffolder): all 7 files listed above.
- Modify: `.../diagnostics/UkrainianLetterUsageDiagnostic.java`
- Modify: the two `.properties` files
- Modify: the two docs `.md` files

- [ ] **Step 1: Run the scaffolder (pipe the tag on stdin)**

Run:
```bash
printf 'suspicious\n' | ./gradlew newDiagnostic \
  --key=UkrainianLetterUsage \
  --nameRu="Использование украинских букв в коде" \
  --nameEn="Using Ukrainian letters in code" \
  --console=plain
```
Expected: `BUILD SUCCESSFUL` and 7 `Created file ...` lines for `UkrainianLetterUsage` / `UkrainianLetterUsageDiagnostic`.

- [ ] **Step 2: Replace the generated class with a compiling skeleton**

The generated stub does not compile (lowercase tag) and extends the wrong base. Overwrite `src/main/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic.java` with:

```java
package com.github._1c_syntax.bsl.languageserver.diagnostics;

import com.github._1c_syntax.bsl.languageserver.diagnostics.metadata.DiagnosticMetadata;
import com.github._1c_syntax.bsl.languageserver.diagnostics.metadata.DiagnosticSeverity;
import com.github._1c_syntax.bsl.languageserver.diagnostics.metadata.DiagnosticTag;
import com.github._1c_syntax.bsl.languageserver.diagnostics.metadata.DiagnosticType;

@DiagnosticMetadata(
  type = DiagnosticType.CODE_SMELL,
  severity = DiagnosticSeverity.MINOR,
  minutesToFix = 5,
  activatedByDefault = false,
  tags = {DiagnosticTag.SUSPICIOUS}
)
public class UkrainianLetterUsageDiagnostic extends AbstractDiagnostic {

  @Override
  protected void check() {
    // implemented in Task 3
  }
}
```

(The license header is added later by `licenseFormat` in Task 7 — do not add it by hand.)

- [ ] **Step 3: Put minimal valid content in the two `.properties` files**

Overwrite `..._ru.properties`:
```properties
diagnosticMessage=Использование украинских букв в коде
diagnosticName=Использование украинских букв в коде
```
Overwrite `..._en.properties`:
```properties
diagnosticMessage=Using Ukrainian letters in code
diagnosticName=Using Ukrainian letters in code
```
(The `letters` parameter line is added in Task 5, together with the parameter field.)

- [ ] **Step 4: Put minimal content in the two docs files (required for the build)**

Ensure `docs/diagnostics/UkrainianLetterUsage.md` keeps its first heading line and has a non-empty description:
```markdown
# Использование украинских букв в коде (UkrainianLetterUsage)

<!-- Блоки выше заполняются автоматически, не трогать -->
## Описание диагностики

В коде не допускается использование украинских букв.
```
Ensure `docs/en/diagnostics/UkrainianLetterUsage.md`:
```markdown
# Using Ukrainian letters in code (UkrainianLetterUsage)

<!-- Блоки выше заполняются автоматически, не трогать -->
## Description

Ukrainian letters are not allowed in code.
```

- [ ] **Step 5: Verify the project compiles**

Run: `./gradlew compileJava compileTestJava --console=plain`
Expected: `BUILD SUCCESSFUL`.

- [ ] **Step 6: Commit the skeleton**

```bash
git add docs/diagnostics/UkrainianLetterUsage.md docs/en/diagnostics/UkrainianLetterUsage.md \
  src/main/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic.java \
  src/test/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnosticTest.java \
  src/main/resources/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic_ru.properties \
  src/main/resources/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic_en.properties \
  src/test/resources/diagnostics/UkrainianLetterUsageDiagnostic.bsl
git commit -m "feat(diagnostics): скелет UkrainianLetterUsage"
```

---

### Task 2: Failing test for the default behavior

**Files:**
- Modify: `src/test/resources/diagnostics/UkrainianLetterUsageDiagnostic.bsl`
- Modify: `src/test/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnosticTest.java`

- [ ] **Step 1: Write the fixture**

Overwrite `src/test/resources/diagnostics/UkrainianLetterUsageDiagnostic.bsl` with exactly (note: keywords stay Russian/English; Ukrainian letters appear only in the comment on line 1, the identifier `Єдність` on line 2, and the procedure name `Підрахунок` on line 5; the string on line 6 contains `і` but must NOT be flagged):

```bsl
// Перевірка вхідних даних
Перем Єдність;
Перем Ключи;

Процедура Підрахунок()
    Message = "Привіт, світ";
КонецПроцедуры

// russian comment is ok
```

- [ ] **Step 2: Write the failing default test**

Overwrite `src/test/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnosticTest.java`:

```java
package com.github._1c_syntax.bsl.languageserver.diagnostics;

import org.eclipse.lsp4j.Diagnostic;
import org.junit.jupiter.api.Test;

import java.util.List;

import static com.github._1c_syntax.bsl.languageserver.util.Assertions.assertThat;

class UkrainianLetterUsageDiagnosticTest extends AbstractDiagnosticTest<UkrainianLetterUsageDiagnostic> {

  UkrainianLetterUsageDiagnosticTest() {
    super(UkrainianLetterUsageDiagnostic.class);
  }

  @Test
  void test() {
    List<Diagnostic> diagnostics = getDiagnostics();

    assertThat(diagnostics).hasSize(3);
    assertThat(diagnostics, true)
      .hasRange(0, 0, 0, 26)
      .hasRange(1, 6, 1, 13)
      .hasRange(4, 10, 4, 20);
  }
}
```

- [ ] **Step 3: Run the test to verify it fails**

Run: `./gradlew test --tests "*UkrainianLetterUsageDiagnosticTest*" --console=plain`
Expected: FAIL — the empty `check()` produces 0 diagnostics, so `hasSize(3)` fails (`expected size: 3 but was: 0`).

---

### Task 3: Implement the check and make the default test pass

**Files:**
- Modify: `src/main/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic.java`

- [ ] **Step 1: Implement the diagnostic**

Overwrite the class with the full implementation (adds the `letters` parameter, `configure`, and the token scan):

```java
package com.github._1c_syntax.bsl.languageserver.diagnostics;

import com.github._1c_syntax.bsl.languageserver.diagnostics.metadata.DiagnosticMetadata;
import com.github._1c_syntax.bsl.languageserver.diagnostics.metadata.DiagnosticParameter;
import com.github._1c_syntax.bsl.languageserver.diagnostics.metadata.DiagnosticSeverity;
import com.github._1c_syntax.bsl.languageserver.diagnostics.metadata.DiagnosticTag;
import com.github._1c_syntax.bsl.languageserver.diagnostics.metadata.DiagnosticType;
import com.github._1c_syntax.bsl.parser.BSLParser;
import com.github._1c_syntax.utils.CaseInsensitivePattern;

import java.util.Map;
import java.util.regex.Pattern;
import java.util.stream.Stream;

@DiagnosticMetadata(
  type = DiagnosticType.CODE_SMELL,
  severity = DiagnosticSeverity.MINOR,
  minutesToFix = 5,
  activatedByDefault = false,
  tags = {DiagnosticTag.SUSPICIOUS}
)
public class UkrainianLetterUsageDiagnostic extends AbstractDiagnostic {

  private static final String DEFAULT_LETTERS = "[іїєґ]";

  @DiagnosticParameter(
    type = String.class,
    defaultValue = DEFAULT_LETTERS
  )
  private Pattern letters = CaseInsensitivePattern.compile(DEFAULT_LETTERS);

  @Override
  public void configure(Map<String, Object> configuration) {
    this.letters = CaseInsensitivePattern.compile(
      (String) configuration.getOrDefault("letters", DEFAULT_LETTERS));
  }

  @Override
  protected void check() {
    Stream.concat(
        documentContext.getTokensFromDefaultChannel().stream()
          .filter(token -> token.getType() == BSLParser.IDENTIFIER),
        documentContext.getComments().stream()
      )
      .filter(token -> letters.matcher(token.getText()).find())
      .forEach(diagnosticStorage::addDiagnostic);
  }
}
```

- [ ] **Step 2: Run the test to verify it passes**

Run: `./gradlew test --tests "*UkrainianLetterUsageDiagnosticTest*" --console=plain`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic.java \
  src/test/resources/diagnostics/UkrainianLetterUsageDiagnostic.bsl \
  src/test/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnosticTest.java
git commit -m "feat(diagnostics): реализация проверки UkrainianLetterUsage"
```

---

### Task 4: Configuration test for the `letters` parameter

**Files:**
- Modify: `src/test/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnosticTest.java`

- [ ] **Step 1: Add the config test method**

Add these imports to the test class:
```java
import java.util.Map;
```
And add this method inside the class (after `test()`):
```java
  @Test
  void testConfigure() {
    Map<String, Object> configuration = diagnosticInstance.info.getDefaultConfiguration();
    configuration.put("letters", "[є]");
    diagnosticInstance.configure(configuration);

    List<Diagnostic> diagnostics = getDiagnostics();

    // Only "Єдність" contains "є"; "Перевірка" and "Підрахунок" contain only "і".
    assertThat(diagnostics).hasSize(1);
    assertThat(diagnostics, true)
      .hasRange(1, 6, 1, 13);
  }
```

- [ ] **Step 2: Run the tests to verify both pass**

Run: `./gradlew test --tests "*UkrainianLetterUsageDiagnosticTest*" --console=plain`
Expected: PASS (2 tests).

- [ ] **Step 3: Commit**

```bash
git add src/test/java/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnosticTest.java
git commit -m "test(diagnostics): тест конфигурации letters для UkrainianLetterUsage"
```

---

### Task 5: Finalize resource bundles (name, message, parameter description)

**Files:**
- Modify: `src/main/resources/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic_ru.properties`
- Modify: `src/main/resources/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic_en.properties`

- [ ] **Step 1: Write the Russian bundle**

Overwrite `..._ru.properties`:
```properties
diagnosticMessage=В коде не допускается использование украинских букв
diagnosticName=Использование украинских букв в коде
letters=Регулярное выражение с набором запрещённых (украинских) букв
```

- [ ] **Step 2: Write the English bundle**

Overwrite `..._en.properties`:
```properties
diagnosticMessage=Ukrainian letters are not allowed in code
diagnosticName=Using Ukrainian letters in code
letters=Regular expression with the set of prohibited (Ukrainian) letters
```

- [ ] **Step 3: Re-run the diagnostic tests (sanity)**

Run: `./gradlew test --tests "*UkrainianLetterUsageDiagnosticTest*" --console=plain`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/main/resources/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic_ru.properties \
  src/main/resources/com/github/_1c_syntax/bsl/languageserver/diagnostics/UkrainianLetterUsageDiagnostic_en.properties
git commit -m "feat(diagnostics): тексты и описание параметра UkrainianLetterUsage"
```

---

### Task 6: Finalize documentation

**Files:**
- Modify: `docs/diagnostics/UkrainianLetterUsage.md`
- Modify: `docs/en/diagnostics/UkrainianLetterUsage.md`

- [ ] **Step 1: Write the Russian docs**

Overwrite `docs/diagnostics/UkrainianLetterUsage.md` (keep the first heading line and the auto-block marker exactly; only edit the sections below the marker):

```markdown
# Использование украинских букв в коде (UkrainianLetterUsage)

<!-- Блоки выше заполняются автоматически, не трогать -->
## Описание диагностики

Код (комментарии и идентификаторы — имена переменных, процедур, функций, параметров и т.п.) должен быть написан на русском или английском языке. Использование украинских букв не допускается.

Так как украинский и русский языки используют общий кириллический алфавит, надёжно отличить украинский текст можно только по буквам, которых нет в русском алфавите: `і`, `ї`, `є`, `ґ` (и их заглавные варианты `І`, `Ї`, `Є`, `Ґ`). Поэтому диагностика срабатывает только при наличии этих букв. Набор букв задаётся параметром `letters` в виде регулярного выражения.

Строковые литералы не проверяются: они часто содержат текст интерфейса, в том числе на украинском языке.

## Примеры

Срабатывает (буква `і` в комментарии, буква `є`/`і` в идентификаторе):

```bsl
// Перевірка вхідних даних
Перем Єдність;
```

Не срабатывает (русский и английский):

```bsl
// Проверка входных данных
Перем Единство;
Сообщить("Привіт, світ"); // текст в строке не проверяется
```

## Источники

<!-- Необходимо указывать ссылки на все источники, из которых почерпнута информация для создания диагностики -->
```

- [ ] **Step 2: Write the English docs**

Overwrite `docs/en/diagnostics/UkrainianLetterUsage.md`:

```markdown
# Using Ukrainian letters in code (UkrainianLetterUsage)

<!-- Блоки выше заполняются автоматически, не трогать -->
## Description

Code (comments and identifiers — variable, procedure, function and parameter names, etc.) must be written in Russian or English. Ukrainian letters are not allowed.

Because Ukrainian and Russian share the Cyrillic alphabet, Ukrainian text can only be reliably distinguished by the letters that do not exist in Russian: `і`, `ї`, `є`, `ґ` (and their uppercase forms `І`, `Ї`, `Є`, `Ґ`). The diagnostic therefore triggers only when these letters are present. The letter set is configured by the `letters` parameter as a regular expression.

String literals are not checked: they often contain interface texts, including in Ukrainian.

## Examples

Triggers (letter `і` in a comment, letter `є`/`і` in an identifier):

```bsl
// Перевірка вхідних даних
Перем Єдність;
```

Does not trigger (Russian and English):

```bsl
// Проверка входных данных
Перем Единство;
Сообщить("Привіт, світ"); // text inside a string is not checked
```

## Sources

<!-- Необходимо указывать ссылки на все источники, из которых почерпнута информация для создания диагностики -->
```

- [ ] **Step 3: Sync the auto-generated header block**

Run: `./gradlew updateDiagnosticDocs --console=plain`
Expected: `BUILD SUCCESSFUL`. This rewrites the heading/auto-block from the diagnostic metadata; `git diff` on the two `.md` files should show only the auto-block being normalized (your description/examples/sources stay intact).

- [ ] **Step 4: Commit**

```bash
git add docs/diagnostics/UkrainianLetterUsage.md docs/en/diagnostics/UkrainianLetterUsage.md
git commit -m "docs(diagnostics): описание UkrainianLetterUsage"
```

---

### Task 7: Regenerate index/schema, apply license headers, run the full build

**Files:**
- Regenerated: `docs/diagnostics/index.md`, `docs/en/diagnostics/index.md`, `.../configuration/schema.json`, `.../configuration/parameters-schema.json`
- Modified: license headers added to the new `.java` files

- [ ] **Step 1: Regenerate the diagnostics index**

Run: `./gradlew updateDiagnosticsIndex --console=plain`
Expected: `BUILD SUCCESSFUL`; `git status` shows `docs/diagnostics/index.md` and `docs/en/diagnostics/index.md` modified (a row for `UkrainianLetterUsage` added).

- [ ] **Step 2: Regenerate the JSON schema**

Run: `./gradlew updateJsonSchema --console=plain`
Expected: `BUILD SUCCESSFUL`; `git status` shows `schema.json` and `parameters-schema.json` modified (the diagnostic and its `letters` parameter added).

- [ ] **Step 3: Apply license headers to the new Java files**

Run: `./gradlew licenseFormat --console=plain`
Expected: `BUILD SUCCESSFUL`; the license header block is prepended to `UkrainianLetterUsageDiagnostic.java` and `UkrainianLetterUsageDiagnosticTest.java`.

- [ ] **Step 4: Run the full build/test suite**

Run: `./gradlew check --console=plain`
Expected: `BUILD SUCCESSFUL`. In particular `DiagnosticInfosTest` passes (name, message, description resource, tags, and the `letters` parameter description for ru + en are all present).

If `check` fails in `DiagnosticInfosTest` for a missing parameter description, re-verify Task 5 added the `letters=` line to **both** `.properties` files.

- [ ] **Step 5: Commit the generated artifacts and license headers**

```bash
git add -A
git commit -m "chore(diagnostics): индекс, json-схема и лицензии для UkrainianLetterUsage"
```

---

## Self-Review notes

- **Spec coverage:** detection set `[іїєґ]` configurable via `letters` (Task 3/5); case-insensitive uppercase coverage (verified in the implementation; the default test's `Єдність` starts with uppercase `Є`); scope = comments + identifiers, strings excluded (Task 3 filters `IDENTIFIER` + `getComments()`); metadata `CODE_SMELL`/`MINOR`/`minutesToFix=5`/`SUSPICIOUS`/`activatedByDefault=false` (Task 1/3); all six artifact files + generated index/schema (Tasks 1–7). Out-of-scope items (string literals, dictionaries, quick fixes) are not implemented.
- **No placeholders:** every code/file step contains full content; commands have expected output.
- **Type/name consistency:** class `UkrainianLetterUsageDiagnostic`, parameter `letters`, key `UkrainianLetterUsage`, and fixture/range values are identical across all tasks and were captured from a real test run (default: 3 diagnostics at `(0,0,0,26)`,`(1,6,1,13)`,`(4,10,4,20)`; config `[є]`: 1 at `(1,6,1,13)`).
